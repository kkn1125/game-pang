declare type BlockSize = {
  x: number;
  y: number;
};
declare type Axis = [number, number];

declare type Animals =
  | "cat"
  | "dog"
  | "lion"
  | "duck"
  | "mouse"
  | "rabbit"
  | "panda"
  | "pig"
  | "racoon";

declare type Condition = {
  type: string;
  amount: number;
};

declare type Result = {
  score: number;
  turn: number;
};
